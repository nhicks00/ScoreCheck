#cloud-config

package_update: true
package_upgrade: false
ssh_pwauth: false
packages:
  - ca-certificates
  - curl
  - jq
  - rsync
  - ufw

write_files:
  - path: /etc/ssh/sshd_config.d/99-scorecheck.conf
    permissions: "0644"
    content: |
      PasswordAuthentication no
      KbdInteractiveAuthentication no
      PermitRootLogin prohibit-password
      X11Forwarding no
      LogLevel VERBOSE
  - path: /etc/sysctl.d/99-scorecheck-monitoring.conf
    permissions: "0644"
    content: |
      net.ipv4.ip_forward=0

runcmd:
  # Firmware metadata refresh is unnecessary on ephemeral event hosts and
  # must not run asynchronously during monitoring qualification.
  - systemctl mask --now fwupd-refresh.timer fwupd-refresh.service || true
  - install -m 0755 -d /etc/apt/keyrings
  - curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
  - chmod a+r /etc/apt/keyrings/docker.asc
  - >-
    echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc]
    https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo $VERSION_CODENAME) stable"
    > /etc/apt/sources.list.d/docker.list
  - apt-get update
  - apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
  - systemctl enable --now docker
  - install -d -m 0750 /opt/scorecheck-monitoring
  - systemctl reload ssh
  - ufw default deny incoming
  - ufw default allow outgoing
  - ufw allow 22/tcp
  - ufw allow 80/tcp
  - ufw allow 443/tcp
  - ufw allow from 172.30.255.0/28 to any port 9108 proto tcp
  - ufw --force enable

final_message: "ScoreCheck observability host bootstrap complete."
